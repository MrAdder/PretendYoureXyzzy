<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>net.socialgamer</groupId>
  <artifactId>pyx</artifactId>
  <version>0.7.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>pyx</name>

  <scm>
    <url>https://github.com/MrAdder/PretendYoureXyzzy</url>
    <connection>scm:git:ssh://git@github.com/MrAdder/PretendYoureXyzzy.git</connection>
  </scm>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>
  </properties>
  
  <repositories>
    <repository>
      <id>hibernatesqlite-maven</id>
      <url>https://hibernate-sqlite.googlecode.com/svn/trunk/mavenrepo</url>
    </repository>
  </repositories>

  <developers>
    <developer>
      <id>ajanata</id>
      <name>Andy Janata</name>
      <email>ajanata@gmail.com</email>
    </developer>
  </developers>

  <build>
    <finalName>ZY</finalName>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.1</version>
        <executions>
          <execution>
            <id>update-js-constants</id>
            <phase>package</phase>
            <goals>
              <goal>java</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <mainClass>net.socialgamer.cah.UpdateJsConstants</mainClass>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>

          <!-- false = true and true = false... http://jira.codehaus.org/browse/MCOMPILER-209 -->
          <useIncrementalCompilation>false</useIncrementalCompilation>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>1.3.1</version>
        <executions>
          <execution>
            <id>enforce-files-exist</id>
            <phase>validate</phase>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireFilesExist>
                  <files>
                    <file>build.properties</file>
                  </files>
                </requireFilesExist>
              </rules>
              <fail>true</fail>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!-- Read database credentials etc. from file. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>properties-maven-plugin</artifactId>
        <version>1.3.0</version>
        <executions>
          <execution>
            <id>read-build-properties</id>
            <phase>initialize</phase>
            <goals>
              <goal>read-project-properties</goal>
            </goals>
          </execution>
          <execution>
            <id>read-report-properties</id>
            <phase>pre-site</phase>
            <goals>
              <goal>read-project-properties</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <files>
            <file>build.properties</file>
          </files>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.5.1</version>

        <configuration>
          <webResources>
            <resource>
              <directory>src/main/resources</directory>
              <filtering>false</filtering>
            </resource>
            <resource>
              <directory>src/main/filtered-resources</directory>
              <filtering>true</filtering>
            </resource>
            <resource>
              <directory>WebContent</directory>
              <filtering>false</filtering>
            </resource>
          </webResources>

          <!-- Web XML is in a filtered folder due to the DTD -->
          <failOnMissingWebXml>false</failOnMissingWebXml>

          <!-- Pack classes into a jar -->
          <archiveClasses>true</archiveClasses>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.jetty</groupId>
        <artifactId>jetty-maven-plugin</artifactId>
        <version>9.4.56.v20240826</version>

        <configuration>
          <!-- To enable JMX in Jetty for debugging, add:
               <jettyXml>${appserver.web-inf.dir}/jetty-jmx.xml</jettyXml> -->
          <webApp>
            <!-- To use a custom Jetty defaults descriptor, add to webApp:
                 <defaultsDescriptor>${project.basedir}/jetty-defaults.xml</defaultsDescriptor> -->
            <descriptor>${project.build.directory}/${project.build.artifact.name}/WEB-INF/web.xml</descriptor>
            <resourceBases>
              <!-- Non-filtered -->
              <resource>src/main/resources</resource>
              <!-- Filtered -->
              <resource>${project.build.directory}/ZY</resource>
            </resourceBases>
            <!-- Jetty hides its own org.eclipse.jetty.* runtime classes from the webapp by default
                 (WebAppContext.__dftServerClasses); DiscordSessionCookieFilter needs
                 org.eclipse.jetty.server.Response and org.eclipse.jetty.http.HttpHeader/HttpFields
                 to rewrite the session cookie's Set-Cookie header for Discord Activity compatibility
                 (see that class's javadoc). This is __dftServerClasses verbatim (confirmed against
                 the pinned jetty-maven-plugin version above) plus two negated entries at the end,
                 carving out just those two subpackages; everything else stays hidden as before. -->
            <serverClasses>
              <serverClass>-org.eclipse.jetty.server.session.SessionData</serverClass>
              <serverClass>-org.eclipse.jetty.jmx.</serverClass>
              <serverClass>-org.eclipse.jetty.util.annotation.</serverClass>
              <serverClass>-org.eclipse.jetty.continuation.</serverClass>
              <serverClass>-org.eclipse.jetty.jndi.</serverClass>
              <serverClass>-org.eclipse.jetty.jaas.</serverClass>
              <serverClass>-org.eclipse.jetty.servlets.</serverClass>
              <serverClass>-org.eclipse.jetty.servlet.DefaultServlet</serverClass>
              <serverClass>-org.eclipse.jetty.servlet.NoJspServlet</serverClass>
              <serverClass>-org.eclipse.jetty.jsp.</serverClass>
              <serverClass>-org.eclipse.jetty.servlet.listener.</serverClass>
              <serverClass>-org.eclipse.jetty.websocket.</serverClass>
              <serverClass>-org.eclipse.jetty.apache.</serverClass>
              <serverClass>-org.eclipse.jetty.util.log.</serverClass>
              <serverClass>-org.eclipse.jetty.alpn.</serverClass>
              <serverClass>org.objectweb.asm.</serverClass>
              <serverClass>org.eclipse.jdt.</serverClass>
              <serverClass>org.eclipse.jetty.</serverClass>
              <serverClass>-org.eclipse.jetty.server.</serverClass>
              <serverClass>-org.eclipse.jetty.http.</serverClass>
            </serverClasses>
          </webApp>
          <scanTargets>
            <scanTarget>src/main/filtered-resources</scanTarget>
          </scanTargets>
          <scanIntervalSeconds>0</scanIntervalSeconds>
          <reload>manual</reload>
          <webAppSourceDirectory>WebContent</webAppSourceDirectory>
          <stopPort>8081</stopPort>
          <stopKey>STOP</stopKey>
          <!-- Newer jetty-maven-plugin versions skip jetty:run on non-war packaging unless told
               otherwise; this project is packaging=jar but still a webapp (see the war-plugin
               config above, and the exploded-war layout jetty:run is pointed at). -->
          <supportedPackagings>
            <supportedPackaging>jar</supportedPackaging>
          </supportedPackagings>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>buildnumber-maven-plugin</artifactId>
        <version>1.4</version>
        <executions>
          <execution>
            <phase>validate</phase>
            <goals>
              <goal>create</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <doCheck>true</doCheck>
          <doUpdate>true</doUpdate>
          <revisionOnScmFailure>0</revisionOnScmFailure>
          <shortRevisionLength>7</shortRevisionLength>
        </configuration>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <!--This plugin's configuration is used to store Eclipse m2e settings 
          only. It has no influence on the Maven build itself. -->
        <plugin>
          <groupId>org.eclipse.m2e</groupId>
          <artifactId>lifecycle-mapping</artifactId>
          <version>1.0.0</version>
          <configuration>
            <lifecycleMappingMetadata>
              <pluginExecutions>
                <pluginExecution>
                  <pluginExecutionFilter>
                    <groupId>
                      org.codehaus.mojo
                    </groupId>
                    <artifactId>
                      properties-maven-plugin
                    </artifactId>
                    <versionRange>
                      [1.0-alpha-2,)
                    </versionRange>
                    <goals>
                      <goal>
                        read-project-properties
                      </goal>
                    </goals>
                  </pluginExecutionFilter>
                  <action>
                    <execute></execute>
                  </action>
                </pluginExecution>
              </pluginExecutions>
            </lifecycleMappingMetadata>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <dependencies>
    <dependency>
      <groupId>com.google.inject</groupId>
      <artifactId>guice</artifactId>
      <version>5.1.0</version>
    </dependency>
    <dependency>
      <groupId>com.google.inject.extensions</groupId>
      <artifactId>guice-assistedinject</artifactId>
      <version>5.1.0</version>
    </dependency>
    <dependency>
      <groupId>com.google.inject.extensions</groupId>
      <artifactId>guice-servlet</artifactId>
      <version>5.1.0</version>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-collections4</artifactId>
      <version>4.1</version>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
      <version>3.20.0</version>
    </dependency>
    <dependency>
      <groupId>org.hibernate.orm</groupId>
      <artifactId>hibernate-core</artifactId>
      <version>6.0.0.Final</version>
    </dependency>
    <dependency>
      <groupId>org.hibernate.orm</groupId>
      <artifactId>hibernate-c3p0</artifactId>
      <version>6.0.0.Final</version>
    </dependency>
    <dependency>
      <groupId>org.hibernate.orm</groupId>
      <artifactId>hibernate-community-dialects</artifactId>
      <version>7.4.7.Final</version>
    </dependency>
    <dependency>
      <groupId>com.dampcake</groupId>
      <artifactId>bencode</artifactId>
      <version>1.3.1</version>
    </dependency>
    <dependency>
      <groupId>com.googlecode.json-simple</groupId>
      <artifactId>json-simple</artifactId>
      <version>1.1</version>
    </dependency>
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>javax.servlet-api</artifactId>
      <version>3.0.1</version>
    </dependency>
    <dependency>
      <!-- Only for DiscordSessionCookieFilter, which has to reach below the servlet API to rewrite
           the session cookie's Set-Cookie header (SameSite/Partitioned aren't expressible through
           javax.servlet.http.Cookie). Provided scope: supplied by jetty-maven-plugin at runtime,
           must match its version exactly since this depends on Jetty's own (non-API) classes. -->
      <groupId>org.eclipse.jetty</groupId>
      <artifactId>jetty-server</artifactId>
      <version>9.4.56.v20240826</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>3.53.4.0</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
      <version>1.6.1</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>1.6.1</version>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-core</artifactId>
      <version>2.26.1</version>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <version>42.7.13</version>
    </dependency>
    <dependency>
      <groupId>org.easymock</groupId>
      <artifactId>easymock</artifactId>
      <version>5.7.0</version>
      <scope>test</scope>
    </dependency>
    <!-- EasyMock 5.7.0 requires byte-buddy 1.18.12; without this, Maven resolves the older
         1.12.8 pulled in transitively by hibernate-core, which is ABI-incompatible. Scope must
         match hibernate-core's own (runtime), otherwise this declaration wins version mediation
         but drops the jar from the runtime classpath/WAR entirely. -->
    <dependency>
      <groupId>net.bytebuddy</groupId>
      <artifactId>byte-buddy</artifactId>
      <version>1.18.12</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>net.sourceforge.findbugs</groupId>
      <artifactId>jsr305</artifactId>
      <version>1.3.2</version>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>org.javassist</groupId>
      <artifactId>javassist</artifactId>
      <version>3.33.0-GA</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.kafka</groupId>
      <artifactId>kafka-clients</artifactId>
      <version>0.10.2.2</version>
    </dependency>
    <dependency>
      <groupId>com.maxmind.geoip2</groupId>
      <artifactId>geoip2</artifactId>
      <version>5.2.0</version>
    </dependency>
    <!-- RegisterHandler and CustomCardsService use these directly; they used to compile only
         because old geoip2 versions pulled them in transitively for its own HTTP client, which
         geoip2 5.x no longer needs (it moved to java.net.http internally). -->
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>httpclient</artifactId>
      <version>4.5.14</version>
    </dependency>
    <dependency>
      <groupId>commons-codec</groupId>
      <artifactId>commons-codec</artifactId>
      <version>1.22.1</version>
    </dependency>
    <dependency>
      <groupId>net.sf.uadetector</groupId>
      <artifactId>uadetector-core</artifactId>
      <version>0.9.22</version>
    </dependency>
    <dependency>
      <groupId>net.sf.uadetector</groupId>
      <artifactId>uadetector-resources</artifactId>
      <version>2014.10</version>
    </dependency>
  </dependencies>
</project>
