# Copy to .env and fill in. Read automatically by `docker compose`.
# Must match hibernate.password in build.properties -- these aren't linked automatically, since
# one is consumed by docker-compose.yml and the other gets baked into the image separately.
POSTGRES_PASSWORD=CorrectHorseBatteryStaple
